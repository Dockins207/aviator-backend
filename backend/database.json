{
  "development": {
    "driver": "pg",
    "host": "localhost",
    "port": 5432,
    "database": "aviator_dev",
    "user": "${DB_USER}",
    "password": "${DB_PASSWORD}",
    "schema": "public"
  },
  "production": {
    "driver": "pg",
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "database": "${DB_NAME}",
    "user": "${DB_USER}",
    "password": "${DB_PASSWORD}",
    "schema": "public"
  }
}
