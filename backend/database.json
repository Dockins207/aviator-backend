{
  "development": {
    "driver": "pg",
    "host": "${DB_HOST}",
    "port": 5432,
    "database": "aviator_db",
    "user": "${DB_USER}",
    "password": "${DB_PASSWORD}",
    "schema": "public"
  },
  "production": {
    "driver": "pg",
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "database": "${DB_NAME}",
    "user": "${DB_USER}",
    "password": "${DB_PASSWORD}",
    "schema": "public"
  }
}
